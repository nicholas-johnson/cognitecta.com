---
interface Props {
  id?: string;
  class?: string;
}

const { id, class: className } = Astro.props;
---

<section
  id={id}
  class:list={['mx-auto max-w-[88rem] px-6 md:px-10 lg:px-16', className]}
>
  <slot />
</section>
